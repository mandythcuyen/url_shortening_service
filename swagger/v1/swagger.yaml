---
openapi: 3.0.1
info:
  title: API V1
  version: v1
  description: URL shortening service
paths:
  "/api/v1/encode":
    post:
      summary: shortens an original URL
      tags:
      - ShortLinks
      parameters: []
      responses:
        '200':
          description: when shortening successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/short_link"
        '422':
          description: when shortening fails
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/error"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                url:
                  type: string
                  format: uri
                  example: https://example.com
              required:
              - url
  "/{short_code}":
    parameters:
    - name: short_code
      in: path
      required: true
      schema:
        type: string
    get:
      summary: Redirect to original URL
      tags:
      - Redirects
      security:
      - Bearer: []
        key: []
      parameters:
      - name: short_code
        in: path
        required: true
        description: The short code to redirect
        schema:
          type: string
      responses:
        '301':
          description: Redirect successful
        '404':
          description: Unsafe URL
components:
  schemas:
    short_link:
      type: object
      properties:
        short_code:
          type: string
          example: GeAi9KU
        short_url:
          type: string
          example: http://localhost:3000/GeAi9KU
          format: uri
    error:
      type: object
      properties:
        error:
          type: array
          items:
            type: string
          example:
          - URL format is invalid
servers:
- url: http://localhost:3000
  variables:
    endPoint:
      default: http://localhost:3000
  description: localhost
